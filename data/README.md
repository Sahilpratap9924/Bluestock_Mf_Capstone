Data folder

- `raw/` contains source CSV files ingested into the project.
- `processed/` contains cleaned and aggregated datasets used for analysis and reporting.

Do not edit raw files in place; add transformation steps to `scripts/` or notebooks and persist outputs to `data/processed/`.
